---
layout   : single
title: 'JavaScript － ゼロサプレス・ゼロパディング！'
published: true
date     : 2010-11-10 00:20:00 +0900
comments : true
categories:
- PC_Tips
- Webサイト
tags:
- JavaScript
---

今日は「ＪａｖａＳｃｒｉｐｔ」で上位桁にゼロのある数値ｊからゼロを削除したり、逆に指定の桁に満たない数値にゼロを付加する方法についてです。

``` javascript
//ゼロサプレス
function zeroSuppress( val ) {
return val.replace( /^0+([0-9]+)/, "$1" );
}
```

``` javascript
//ゼロパディング
function zeroPudding( val ) {
return ( "0" + val ).slice( -2 )
}
```

※上記のゼロパディングは２桁の例。

実際には当方は「onfocus」（テキストボックス編集）時にゼロサプレス関数、「onblur」（ロストフォーカス）時にゼロパディング関数を呼び出して使用しています。

以上です。

