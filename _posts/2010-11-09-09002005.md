---
layout   : single
title: 'JavaScript － カンマ編集！'
published: true
date     : 2010-11-09 00:20:00 +0900
comments : true
categories:
- PC_Tips
- Webサイト
tags:
- JavaScript
---

今日は「ＪａｖａＳｃｒｉｐｔ」で数値を３桁ごとのカンマ区切りにしたり、逆にカンマ付き数値からカンマを削除する方法についてです。

``` javascript
//カンマ挿入関数
function insertComma( str ) {
var num = new String( str ).replace( /,/g, "" );
while ( num != ( num = num.replace( /^(-?\d+)(\d{3})/, "$1,$2" ) ) );
return num;
}
```

``` javascript
//カンマ削除関数
function deleteComma( str ) {
var num = new String( str ).replace( /,/g, "" );
return num;
}
```

実際には当方は「onfocus」（テキストボックス編集）時にカンマ削除関数、「onblur」（ロストフォーカス）時にカンマ挿入関数を呼び出して使用しています。

以上です。

