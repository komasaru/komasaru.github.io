---
layout   : single
title: 'Ｆｅｄｏｒａ１２ － Ｓａｍｂａエラー！'
published: true
date     : 2009-12-26 00:20:00 +0900
comments : true
categories:
- サーバ構築
tags:
- Fedora
- Samba
---

今日ふと、
うちのＦｅｄｏｒａ１２サーバのログ [ /var/log/messages ]を
見てみると
こんなメッセージがでているのを発見しました。

``` text
Dec 17 11:14:24 pulsar smbd[30572]:   Unable to connect to CUPS server localhost:631
- 接続を拒否されました
```

ＣＵＰＳサーバに接続できない、見たいなエラー。

<!--more-->

ｓａｍｂａの設定[ /etc/samba/smb.conf ]では

``` text
load printers = no
disable spoolss = yes
```

としているが・・・

と思って調べてみました。

なにやら、[ /etc/samba/smb.conf ]の

``` text
printing = cups
```

がかかわっているようです。
これだと、プリンタ設定していないのにＣＵＰＳサーバを探しに行ってエラーになっていたようです。

[ /etc/samba/smb.conf ]のこの部分を
以下のようにすればよいみたいです。

``` text
printing = bsd
```

理由は元のＣソース内で、
printing が [ bsd ] 以外の場合、規定値[ cups ]を返す
ようになっているからみたいです。

これで、エラーは出なくなりました。

では、また。

